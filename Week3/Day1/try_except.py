# TRY AND EXCEPT

try:
    move = int(input('Give ur move from 1 - 9: ')) # we write what we want to try, like, converting to int
    if move > 9:
        raise Exception('Choose a number in range 1 - 9')

    except 
